<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tmasc.hangmanthegame_1">

    <!-- - tilladelser -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HelpActivity" />
        <activity android:name=".activities.HighscoreActivity" />
        <activity android:name=".activities.GameActivity" />
        <activity android:name=".activities.LostGameActivity" />
        <activity android:name=".activities.WonGameActivity" />
        <activity android:name=".activities.HighscoreCreateScoreActivity" />
        <activity android:name=".activities.ChooseGameActivity" />
        <activity android:name=".activities.ChooseWordActivity"></activity>
    </application>

</manifest>