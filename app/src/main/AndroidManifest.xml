<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tmasc.hangmanthegame_1">

    <!-- - tilladelser -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.HelpActivity" />
        <activity android:name=".main.HighscoreActivity" />
        <activity android:name=".main.GameActivity" />
        <activity android:name=".main.LostGameActivity" />
        <activity android:name=".main.WonGameActivity" />
        <activity android:name=".main.HighscoreCreateScoreActivity" />
        <activity android:name=".main.ChooseGameActivity" />
        <activity android:name=".main.ChooseWordActivity"></activity>
    </application>

</manifest>